# Secrets — never commit
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# WeRead sync artifacts
bookmark.json
# NOTE: OUT_FOLDER/ is NOT ignored — we need the heatmap SVG
# to be committed so raw.githubusercontent.com can serve it
# for the Notion embed.

# Build / dist
build/
dist/
